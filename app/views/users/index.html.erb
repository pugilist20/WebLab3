<% provide(:title, "All Users") %>
<h1>All Users</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <strong><%= user.name %></strong> — <%= user.email %>
      <small>(ID: <%= user.id %>)</small>
    </li>
  <% end %>
</ul>